<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <application
        android:icon="@mipmap/app_icon"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:icon="@mipmap/app_icon"
            android:label="DeviceTool">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="distractionOptimized"
                android:value="true" />
        </activity>
        <activity
            android:name=".DeviceToolActivity"
            android:configChanges="assetsPaths|orientation|density|fontScale|layoutDirection|locale|screenLayout|screenSize"
            android:exported="false"
            android:icon="@mipmap/app_icon"
            android:label="DeviceTool">
            <meta-data
                android:name="distractionOptimized"
                android:value="true" />
        </activity>
        <activity
            android:name=".TestActivity"
            android:exported="true"
            android:icon="@android:color/holo_green_light"
            android:label="LottieSample">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="distractionOptimized"
                android:value="true" />
        </activity>
    </application>

</manifest>